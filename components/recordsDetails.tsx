import { ScrollView, Text, View } from 'react-native';

export default function RecordsDetails() {
    return(
        <ScrollView>
            <View>
                <Text>
                    Recordsssss
                </Text>
            </View>
        </ScrollView>
    )
}